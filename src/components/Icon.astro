---
import fetch from "~/utils/cache";
const icons = await fetch('https://cdndevolutions.blob.core.windows.net/devolutions-icons/3.4.1/inline-svg/devolutions-icons.json');

const { name } = Astro.props;
const icon = icons[name];
---

<Fragment set:html={icon} />